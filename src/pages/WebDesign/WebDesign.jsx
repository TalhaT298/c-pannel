import React from 'react';
import { Link } from 'react-router-dom';

const WebDesign = () => {
    return (
        <Link to="webdesign">
        <div>
            this is web design
        </div>
        </Link>
    );
};

export default WebDesign;