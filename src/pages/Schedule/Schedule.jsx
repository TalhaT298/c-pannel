import React from 'react';
import { Link } from 'react-router-dom';

const Schedule = () => {
    return (
        <div>
            <Link to="/sched"></Link>
            ok
        </div>
    );
};

export default Schedule;